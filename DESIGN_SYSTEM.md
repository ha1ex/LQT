# Дизайн-система Life Quality Tracker

Версия: 1.0  
Дата обновления: 2024

## Обзор

Дизайн-система Life Quality Tracker — это современная, целостная система визуального дизайна, созданная для создания интуитивно понятного и эмоционально привлекательного пользовательского интерфейса. Система построена на принципах доступности, последовательности и премиального пользовательского опыта.

### Принципы дизайна

1. **Чистота и минимализм** — Использование пространства для создания легкого и воздушного интерфейса
2. **Эмоциональная связь** — Использование цвета и анимации для создания позитивного опыта
3. **Доступность** — Высокий контраст и читаемость для всех пользователей
4. **Премиальность** — Утонченные градиенты, тени и микроанимации
5. **Мобильность** — Адаптивный дизайн с фокусом на mobile-first

---

## Цветовая палитра

### Основные цвета

#### Светлая тема
- **Background**: `hsl(0 0% 98%)` — Основной фон
- **Foreground**: `hsl(224 71% 4%)` — Основной текст
- **Primary**: `hsl(217 91% 60%)` — Основной акцентный цвет (синий)
- **Primary Foreground**: `hsl(0 0% 98%)` — Текст на основном цвете

#### Темная тема
- **Background**: `hsl(222 84% 4.9%)` — Основной фон
- **Foreground**: `hsl(210 40% 98%)` — Основной текст
- **Primary**: `hsl(217 91% 60%)` — Основной акцентный цвет (синий)
- **Primary Foreground**: `hsl(222 84% 4.9%)` — Текст на основном цвете

### Статусные цвета

#### Универсальные (светлая и темная тема)
- **Success**: `hsl(142 76% 36%)` — Зеленый для успеха
- **Warning**: `hsl(38 92% 50%)` — Оранжевый для предупреждений
- **Error**: `hsl(0 84% 60%)` — Красный для ошибок
- **Info**: `hsl(217 91% 60%)` — Синий для информации

### Нейтральные цвета

#### Светлая тема
- **Secondary**: `hsl(220 14% 96%)` — Вторичный фон
- **Muted**: `hsl(220 14% 96%)` — Приглушенный фон
- **Muted Foreground**: `hsl(220 9% 46%)` — Приглушенный текст
- **Border**: `hsl(220 13% 91%)` — Границы элементов

#### Темная тема
- **Secondary**: `hsl(217 32% 17%)` — Вторичный фон
- **Muted**: `hsl(217 32% 17%)` — Приглушенный фон
- **Muted Foreground**: `hsl(215 20% 65%)` — Приглушенный текст
- **Border**: `hsl(217 32% 17%)` — Границы элементов

### Градиенты

- **Primary Gradient**: `linear-gradient(135deg, hsl(217 91% 60%) 0%, hsl(217 91% 70%) 100%)`
- **Success Gradient**: `linear-gradient(135deg, hsl(142 76% 36%) 0%, hsl(142 76% 46%) 100%)`
- **Card Gradient**: `linear-gradient(135deg, hsl(0 0% 100%) 0%, hsl(220 14% 99%) 100%)`

---

## Типографика

### Система шрифтов
- **Primary**: Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif
- **Настройки**: antialiased для улучшенного рендеринга

### Размеры заголовков
- **H1**: `text-3xl` (30px) — Основные заголовки страниц
- **H2**: `text-2xl` (24px) — Заголовки секций
- **H3**: `text-xl` (20px) — Заголовки карточек
- **H4**: `text-lg` (18px) — Подзаголовки
- **H5**: `text-base` (16px) — Мелкие заголовки
- **H6**: `text-sm` (14px) — Метки и подписи

### Весы шрифтов
- **Light**: `font-light` (300) — Редко используется
- **Normal**: `font-normal` (400) — Основной текст
- **Medium**: `font-medium` (500) — Акцентный текст
- **Semibold**: `font-semibold` (600) — Подзаголовки
- **Bold**: `font-bold` (700) — Заголовки

### Размеры текста
- **XS**: `text-xs` (12px) — Метки, подписи
- **SM**: `text-sm` (14px) — Вторичный текст
- **Base**: `text-base` (16px) — Основной текст
- **LG**: `text-lg` (18px) — Акцентный текст

---

## Система компонентов

### Карточки

#### Card Premium
```css
.card-premium {
  background: var(--gradient-card);
  border: none;
  box-shadow: var(--shadow-lg);
  border-radius: 1rem;
  transition: all 0.5s;
}
```
**Применение**: Основные информационные блоки, дашборд
**Особенности**: 
- Градиентный фон
- Эффект поднятия при hover
- Топ-граница с градиентом при hover

#### Card Hero
```css
.card-hero {
  background: var(--gradient-primary);
  border-radius: 1rem;
  box-shadow: var(--shadow-xl);
}
```
**Применение**: Главные CTA-блоки, приветственные секции

#### Card Modern
```css
.card-modern {
  background: var(--card);
  border: 1px solid var(--border);
  border-radius: 0.75rem;
  box-shadow: var(--shadow-md);
}
```
**Применение**: Стандартные информационные блоки

### Кнопки

#### Primary Button
```css
.btn-primary {
  background: var(--gradient-primary);
  color: white;
  border-radius: 0.75rem;
  font-weight: 600;
  box-shadow: var(--shadow-md);
  transition: all 0.2s;
}
```
**Состояния**:
- Default: Градиентный фон
- Hover: Поднятие на 1px, увеличенная тень
- Active: Возвращение в исходное положение

#### Modern Button
```css
.btn-modern {
  background: var(--background);
  border: 1px solid var(--border);
  color: var(--foreground);
  border-radius: 0.75rem;
  font-weight: 500;
}
```
**Применение**: Вторичные действия, навигация

#### Icon Button
```css
.btn-icon {
  width: 3rem;
  height: 3rem;
  border-radius: 0.75rem;
  transition: all 0.3s;
}
```
**Особенности**: Эффект масштабирования при hover

### Система рейтингов

#### Rating Button
```css
.rating-button {
  height: 3rem;
  border-radius: 0.75rem;
  border: 2px solid;
  font-weight: 600;
  transition: all 0.2s;
}
```

**Состояния**:
- **Selected**: Градиентный фон, белый текст, масштаб 1.05
- **Unselected**: Приглушенный фон, серый текст

### Статусные индикаторы

#### Status Indicator
```css
.status-indicator {
  padding: 0.375rem 0.75rem;
  border-radius: 9999px;
  font-size: 0.75rem;
  font-weight: 600;
}
```

**Типы**:
- **Excellent**: Зеленый фон `bg-emerald-100`, темно-зеленый текст
- **Good**: Синий фон `bg-blue-100`, темно-синий текст  
- **Warning**: Желтый фон `bg-amber-100`, темно-желтый текст
- **Poor**: Красный фон `bg-red-100`, темно-красный текст

### Прогресс-бары

#### Progress Bar
```css
.progress-bar {
  width: 100%;
  height: 0.5rem;
  background: var(--muted);
  border-radius: 9999px;
}

.progress-fill {
  height: 100%;
  background: var(--gradient-primary);
  border-radius: 9999px;
  transition: all 0.5s;
}
```

---

## Система теней

### Иерархия теней

- **XS**: `0 1px 2px 0 rgb(0 0 0 / 0.05)` — Минимальная тень
- **SM**: `0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1)` — Мелкие элементы
- **MD**: `0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1)` — Карточки
- **LG**: `0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1)` — Модальные окна
- **XL**: `0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1)` — Floating элементы

### Темная тема
В темной теме тени имеют повышенную непрозрачность (0.2-0.3 вместо 0.1) для лучшей видимости.

---

## Система анимаций

### Основные анимации

#### Fade In
```css
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```
**Применение**: Появление контента, плавные переходы

#### Slide Up
```css
@keyframes slideUp {
  from {
    opacity: 0;
    transform: translateY(40px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```
**Применение**: Карточки, секции контента

#### Scale In
```css
@keyframes scaleIn {
  from {
    opacity: 0;
    transform: scale(0.9);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}
```
**Применение**: Модальные окна, попапы

#### Bounce In
```css
@keyframes bounceIn {
  0% { opacity: 0; transform: scale(0.3); }
  50% { opacity: 1; transform: scale(1.05); }
  70% { transform: scale(0.9); }
  100% { opacity: 1; transform: scale(1); }
}
```
**Применение**: Уведомления, достижения

### Утилитарные классы анимаций

- `.animate-fade-in` — Плавное появление за 0.5s
- `.animate-slide-up` — Скольжение снизу за 0.6s
- `.animate-scale-in` — Масштабирование за 0.4s
- `.animate-bounce-in` — Упругое появление за 0.6s

### Микроанимации

#### Hover эффекты
- **Карточки**: Поднятие на 4px с увеличенной тенью
- **Кнопки**: Поднятие на 1px
- **Иконки**: Масштабирование до 1.05

#### Transition timing
- **Быстрые**: `duration-200` (0.2s) — Кнопки, ховеры
- **Средние**: `duration-300` (0.3s) — Карточки, навигация
- **Медленные**: `duration-500` (0.5s) — Сложные переходы

---

## Система сеток и отступов

### Сетка

#### Основная сетка
- **Container**: Центрированный с отступами `2rem`
- **Breakpoints**: 
  - `sm`: 640px
  - `md`: 768px
  - `lg`: 1024px
  - `xl`: 1280px
  - `2xl`: 1400px

#### Grid системы
- **Dashboard**: `grid-cols-1 lg:grid-cols-3` — Мобильная к трехколоночной
- **Cards**: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` — Адаптивные карточки
- **Content**: `grid-cols-1 lg:grid-cols-2` — Контент + сайдбар

### Отступы

#### Внутренние отступы (padding)
- **XS**: `p-2` (8px) — Мелкие элементы
- **SM**: `p-4` (16px) — Кнопки, badge
- **MD**: `p-6` (24px) — Карточки
- **LG**: `p-8` (32px) — Секции
- **XL**: `p-12` (48px) — Контейнеры

#### Внешние отступы (margin)
- **Gap Small**: `gap-4` (16px) — Элементы в группе
- **Gap Medium**: `gap-6` (24px) — Секции
- **Gap Large**: `gap-8` (32px) — Основные блоки

#### Touch targets
- **Минимальный размер**: `44px` — Для мобильных интерфейсов
- **Класс**: `min-h-touch-target` — Применяется к интерактивным элементам

---

## Иконография

### Система иконок

#### Lucide React
**Библиотека**: lucide-react  
**Стиль**: Outline, минималистичный  
**Размеры**:
- **SM**: `h-4 w-4` (16px) — Инлайн иконки
- **MD**: `h-5 w-5` (20px) — Кнопки, навигация
- **LG**: `h-6 w-6` (24px) — Заголовки
- **XL**: `h-8 w-8` (32px) — Крупные элементы

#### Категории иконок
- **Навигация**: Home, Menu, ChevronRight, ArrowLeft
- **Действия**: Plus, Edit, Trash, Save, Share
- **Статус**: Check, X, Alert, Info
- **Контент**: Calendar, Clock, User, Heart
- **Аналитика**: TrendingUp, BarChart, PieChart

### Использование эмодзи

#### Рейтинговая система
- **😢** — Очень плохо (1)
- **😕** — Плохо (2)  
- **😐** — Нормально (3)
- **😊** — Хорошо (4)
- **😄** — Отлично (5)

**Размер**: `text-xl` (20px) в кнопках рейтинга

---

## Формы и ввод данных

### Input поля

#### Стандартный Input
```css
.input {
  height: 2.5rem;
  padding: 0.5rem 0.75rem;
  border: 1px solid var(--input);
  border-radius: 0.375rem;
  background: var(--background);
  font-size: 0.875rem;
}
```

**Состояния**:
- **Default**: Серая граница
- **Focus**: Синяя граница с ring эффектом
- **Error**: Красная граница
- **Disabled**: Пониженная opacity

### Label
```css
.label {
  font-size: 0.875rem;
  font-weight: 500;
  color: var(--foreground);
}
```

### Checkbox
```css
.checkbox {
  width: 1rem;
  height: 1rem;
  border: 1px solid var(--primary);
  border-radius: 0.125rem;
}
```

**Состояния**:
- **Unchecked**: Прозрачный фон, синяя граница
- **Checked**: Синий фон, белая галочка

---

## Адаптивность

### Mobile-first подход

#### Принципы
1. **Сначала мобильный** — Дизайн начинается с мобильной версии
2. **Progressive Enhancement** — Добавление функций для больших экранов
3. **Touch-friendly** — Минимум 44px для интерактивных элементов

#### Breakpoint стратегия
- **Mobile**: < 768px — Одна колонка, вертикальная навигация
- **Tablet**: 768px-1024px — Две колонки, сайдбар
- **Desktop**: > 1024px — Три колонки, горизонтальная навигация

#### Мобильные компоненты
- **Bottom Navigation** — Нижняя навигация для мобильных
- **Mobile Header** — Компактный заголовок с hamburger меню
- **Drawer** — Выдвижные панели

### Адаптивные утилиты

#### Видимость
- `md:hidden` — Скрыть на больших экранах
- `hidden md:block` — Показать только на больших экранах

#### Сетка
- `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` — Адаптивные колонки
- `flex-col md:flex-row` — Вертикальный на мобильном, горизонтальный на десктопе

---

## Состояния и обратная связь

### Состояния загрузки

#### Skeleton
```css
.skeleton {
  background: linear-gradient(90deg, var(--muted) 25%, var(--muted-foreground) 50%, var(--muted) 75%);
  animation: loading 1.5s infinite;
}
```

#### Spinner
**Компонент**: Lucide React `Loader2` с анимацией вращения

### Toast уведомления

#### Типы
- **Success**: Зеленый цвет, Check иконка
- **Error**: Красный цвет, X иконка  
- **Warning**: Оранжевый цвет, Alert иконка
- **Info**: Синий цвет, Info иконка

#### Позиционирование
- **Desktop**: Правый верхний угол
- **Mobile**: Центр экрана

### Progress индикаторы

#### Linear Progress
```css
.progress {
  width: 100%;
  height: 8px;
  background: var(--muted);
  border-radius: 4px;
  overflow: hidden;
}
```

#### Circular Progress
**Реализация**: SVG с анимированным stroke-dashoffset

---

## Accessibility (A11Y)

### Цветовая доступность

#### Контрастность
- **AA уровень**: 4.5:1 для нормального текста
- **AAA уровень**: 7:1 для мелкого текста
- **Крупный текст**: 3:1 минимум

#### Color-blind friendly
- Не полагаемся только на цвет для передачи информации
- Используем иконки и текст вместе с цветом
- Тестируем с различными типами цветовой слепоты

### Клавиатурная навигация

#### Focus states
```css
.focus-visible {
  outline: 2px solid var(--ring);
  outline-offset: 2px;
}
```

#### Tab order
- Логический порядок табуляции
- Пропуск декоративных элементов
- Focus trap в модальных окнах

### Screen readers

#### Semantic HTML
- Используем правильные HTML теги
- ARIA-labels для интерактивных элементов
- Heading hierarchy (h1, h2, h3...)

#### Live regions
```html
<div aria-live="polite">Статус обновлен</div>
<div aria-live="assertive">Ошибка валидации</div>
```

---

## Производительность

### Оптимизация CSS

#### Critical CSS
- Inline критический CSS
- Async загрузка некритического CSS
- CSS минификация

#### Unused CSS
- PurgeCSS для удаления неиспользуемых стилей
- Tree-shaking в Tailwind CSS

### Анимации

#### GPU Acceleration
```css
.gpu-accelerated {
  transform: translateZ(0);
  will-change: transform;
}
```

#### Performance budget
- Максимум 3 одновременные анимации
- Длительность до 300ms для UI
- 60fps для плавности

### Images

#### Responsive images
```html
<img 
  src="image-800w.jpg" 
  srcset="image-400w.jpg 400w, image-800w.jpg 800w"
  sizes="(max-width: 768px) 100vw, 50vw"
  alt="Описание"
/>
```

---

## Руководство по использованию

### Именование классов

#### BEM методология для кастомных компонентов
```css
.life-tracker__card { }
.life-tracker__card--premium { }
.life-tracker__card__title { }
```

#### Tailwind утилиты
- Используем utility-first подход
- Комбинируем с CSS переменными для консистентности

### Порядок CSS свойств

#### Рекомендованный порядок
1. Позиционирование (position, top, left...)
2. Box model (display, width, height, margin, padding...)
3. Типографика (font, text-align, line-height...)
4. Визуальные эффекты (background, border, box-shadow...)
5. Анимации (transition, animation...)

### Кодстайл

#### CSS переменные
```css
/* ✅ Правильно */
color: hsl(var(--primary));

/* ❌ Неправильно */
color: #3b82f6;
```

#### Tailwind классы
```html
<!-- ✅ Правильно -->
<div className="card-premium p-6 animate-fade-in">

<!-- ❌ Неправильно -->  
<div className="bg-white border border-gray-200 shadow-lg">
```

---

## Заключение

Эта дизайн-система обеспечивает целостный и профессиональный пользовательский опыт для Life Quality Tracker. Следование этим руководящим принципам гарантирует консистентность, доступность и производительность всего приложения.

### Обновления

Дизайн-система является живым документом и должна обновляться по мере развития продукта. Все изменения должны быть документированы и коммуницированы команде.

### Контакты

Для вопросов по дизайн-системе или предложений по улучшению, пожалуйста, свяжитесь с командой дизайна.