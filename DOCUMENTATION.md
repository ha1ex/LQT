
# Документация Life Quality Tracker

## Обзор приложения

**Life Quality Tracker** - это веб-приложение для отслеживания качества жизни, построенное на React с использованием современного стека технологий (Vite, TypeScript, Tailwind CSS, shadcn-ui).

Приложение позволяет пользователям:
- Оценивать различные аспекты жизни по 10-бальной шкале
- Отслеживать корреляции между метриками
- Получать персональные инсайты и рекомендации
- Ставить цели и отслеживать прогресс
- Анализировать тренды качества жизни

## Архитектура приложения

### Основные компоненты

1. **LifeQualityTracker** (`src/components/LifeQualityTracker.tsx`) - главный компонент приложения
2. **Tracker Components** (`src/components/tracker/`) - специализированные компоненты для различных функций
3. **UI Components** (`src/components/ui/`) - переиспользуемые компоненты интерфейса

### Структура файлов

```
src/
├── components/
│   ├── LifeQualityTracker.tsx    # Главный компонент
│   ├── tracker/
│   │   ├── Breadcrumbs.tsx       # Навигационные крошки
│   │   ├── CategoryBadge.tsx     # Бейджи категорий
│   │   ├── CorrelationAnalysis.tsx # Анализ корреляций
│   │   ├── MetricNotes.tsx       # Заметки к метрикам
│   │   ├── PersonalGoals.tsx     # Система целей
│   │   ├── PersonalInsights.tsx  # Персональные инсайты
│   │   ├── PersonalRecommendations.tsx # Рекомендации
│   │   ├── QuickEmojiRating.tsx  # Быстрая оценка эмодзи
│   │   ├── StreakSystem.tsx      # Система стриков
│   │   └── WeeklyInsights.tsx    # Недельная аналитика
│   └── ui/                       # Компоненты интерфейса
├── hooks/
│   ├── use-mobile.tsx            # Хук для определения мобильного устройства
│   └── use-toast.ts              # Хук для уведомлений
├── lib/
│   └── utils.ts                  # Утилиты
└── pages/
    └── Index.tsx                 # Главная страница
```

## Структура данных

### Метрики (Metrics)
```typescript
interface Metric {
  id: string;        // Уникальный идентификатор
  name: string;      // Название метрики
  icon: string;      // Эмодзи иконка
  description: string; // Описание
  category: string;  // Категория (mental, health, relationships, etc.)
  isCustom: boolean; // Пользовательская или системная
}
```

### Еженедельные данные
```typescript
interface WeekData {
  week: string;      // Номер недели (W14, W15, etc.)
  date: string;      // Диапазон дат
  overall: number;   // Общий индекс (рассчитывается автоматически)
  [metricName: string]: number; // Оценки по каждой метрике (1-10)
}
```

### Цели (Goals)
```typescript
interface Goal {
  id: string;
  metricName: string;
  targetValue: number;
  currentValue: number;
  isCompleted: boolean;
  createdAt: string;
  completedAt?: string;
}
```

## Функциональные возможности

### 1. Система оценки метрик
- **Стандартная оценка**: Шкала 1-10 с кнопками для каждого значения
- **Быстрая оценка эмодзи**: Упрощенная система с 5 эмодзи (1,3,5,7,9)
- **Заметки к метрикам**: Возможность добавлять текстовые комментарии

### 2. Категории метрик
- **mental** - Ментальное здоровье (🧠)
- **health** - Физическое здоровье (💪)
- **relationships** - Отношения (❤️)
- **finance** - Финансы (💰)
- **social** - Социальная активность (👥)
- **personal** - Личностное развитие (🌱)
- **lifestyle** - Образ жизни (🏠)
- **custom** - Пользовательские категории (⚙️)

### 3. Система корреляций
- Расчет корреляции Пирсона между метриками
- Отображение только значимых корреляций (|r| > 0.6)
- Визуализация с помощью bar chart
- Интерпретация силы связи:
  - **Сильная**: |r| > 0.7
  - **Умеренная**: 0.4 < |r| ≤ 0.7
  - **Слабая**: |r| ≤ 0.4

### 4. Персональные инсайты
- Анализ взаимосвязей между метриками
- Выявление положительных и отрицательных корреляций
- Автоматическое описание найденных паттернов
- Рекомендации на основе данных

### 5. Система рекомендаций
- Анализ низких метрик (< 5 баллов)
- Мониторинг трендов (падение общего индекса)
- Персонализированные советы для каждой метрики
- Приоритизация по критичности:
  - **Высокий**: Критические проблемы
  - **Средний**: Области для улучшения
  - **Низкий**: Профилактические меры

### 6. Система целей
- Создание целей для отдельных метрик
- Автоматическое отслеживание прогресса
- Уведомления о достижении целей
- Сохранение в localStorage

### 7. Система стриков
- Отслеживание последовательности оценок
- Текущий и лучший стрик
- Недельные цели и прогресс
- Мотивационные сообщения

## Алгоритмы и расчеты

### Расчет общего индекса
```typescript
const calculateOverallIndex = (weekData: any, metrics: any[]) => {
  const values = metrics
    .map(metric => weekData[metric.name])
    .filter(value => value !== undefined && value !== null);
  
  return values.length > 0 ? values.reduce((sum, val) => sum + val, 0) / values.length : 0;
};
```

### Корреляция Пирсона
```typescript
const calculatePearsonCorrelation = (x: number[], y: number[]): number => {
  if (x.length !== y.length || x.length === 0) return 0;
  
  const n = x.length;
  const sumX = x.reduce((a, b) => a + b, 0);
  const sumY = y.reduce((a, b) => a + b, 0);
  const sumXY = x.reduce((total, xi, i) => total + xi * y[i], 0);
  const sumXX = x.reduce((total, xi) => total + xi * xi, 0);
  const sumYY = y.reduce((total, yi) => total + yi * yi, 0);
  
  const numerator = n * sumXY - sumX * sumY;
  const denominator = Math.sqrt((n * sumXX - sumX * sumX) * (n * sumYY - sumY * sumY));
  
  return denominator === 0 ? 0 : Math.max(-1, Math.min(1, numerator / denominator));
};
```

### Расчет трендов
```typescript
const calculateTrend = (data: number[]): 'up' | 'down' | 'stable' => {
  if (data.length < 2) return 'stable';
  
  const recent = data.slice(-3);
  const earlier = data.slice(-6, -3);
  
  const recentAvg = recent.reduce((a, b) => a + b, 0) / recent.length;
  const earlierAvg = earlier.reduce((a, b) => a + b, 0) / earlier.length;
  
  const diff = recentAvg - earlierAvg;
  
  if (diff > 0.5) return 'up';
  if (diff < -0.5) return 'down';
  return 'stable';
};
```

## Состояние приложения

### Основное состояние
- `currentView` - активная вкладка ('overview' | 'analysis' | 'goals' | 'insights' | 'settings')
- `timeFilter` - временной фильтр ('week' | 'month' | 'quarter' | 'year')
- `categoryFilter` - фильтр по категориям
- `weekRatings` - оценки текущей недели
- `metricNotes` - заметки к метрикам
- `customMetrics` - пользовательские метрики

### Персистентные данные
- Цели сохраняются в `localStorage` под ключом `lqt_goals`
- Заметки к метрикам сохраняются в `localStorage`
- Автоматическая загрузка при инициализации приложения

## Адаптивность и UX

### Мобильная версия (< 768px)
- Нижняя навигация с 5 основными разделами
- Оптимизированные размеры компонентов
- Жесты swipe для навигации
- Упрощенная верстка для экономии места

### Десктопная версия (≥ 768px)
- Боковая панель навигации
- Расширенная аналитика в несколько колонок
- Hover-эффекты и анимации
- Расширенная функциональность

### Анимации
- Плавные переходы между состояниями
- Последовательная анимация элементов с задержкой
- Микроинтерактивности для улучшения UX
- Использование `framer-motion` для сложных анимаций

## Технические особенности

### Оптимизация производительности
- `useMemo` для тяжелых вычислений
- `useCallback` для стабильности функций
- Ленивая загрузка данных
- Виртуализация для больших списков

### Обработка ошибок
- Fallback значения для отсутствующих данных
- Проверки на undefined/null
- Graceful degradation для компонентов
- Error boundaries для критических ошибок

### Цветовая система
- Семантические цвета для состояний
- Адаптивная цветовая схема
- Accessibility-friendly контрасты
- CSS custom properties для темизации

## Используемые технологии

### Основной стек
- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик и dev server
- **Tailwind CSS** - утилитарные стили
- **shadcn-ui** - компоненты интерфейса

### Дополнительные библиотеки
- **@radix-ui** - accessible компоненты
- **lucide-react** - иконки
- **recharts** - графики и диаграммы
- **date-fns** - работа с датами
- **@tanstack/react-query** - управление состоянием сервера

### Инструменты разработки
- **ESLint** - линтер
- **Prettier** - форматирование кода
- **Husky** - git hooks
- **TypeScript** - статическая типизация

## Развертывание

### Локальная разработка
```bash
npm install
npm run dev
```

### Сборка для продакшена
```bash
npm run build
npm run preview
```

### Развертывание
- Поддержка развертывания на Vercel, Netlify
- Автоматическая генерация статических файлов
- Оптимизация для производительности

## Безопасность

### Валидация данных
- Проверка типов на уровне TypeScript
- Валидация пользовательского ввода
- Санитизация данных

### Приватность
- Локальное хранение данных
- Отсутствие передачи данных на сервер
- Соблюдение GDPR требований

## Поддержка и обновления

### Версионирование
- Semantic versioning
- Changelog для каждой версии
- Миграции для обновлений

### Тестирование
- Unit тесты для компонентов
- Integration тесты для workflow
- E2E тесты для критических путей

## Известные ограничения

1. **Локальное хранение** - данные хранятся только в браузере
2. **Отсутствие синхронизации** - нет облачного бэкенда
3. **Браузерная совместимость** - поддержка современных браузеров
4. **Офлайн режим** - ограниченная функциональность без интернета

## Планы развития

### Краткосрочные цели
- Экспорт данных в CSV/JSON
- Импорт данных из других трекеров
- Расширенная аналитика

### Долгосрочные цели
- Облачная синхронизация
- Мобильное приложение
- Интеграция с внешними сервисами
- Машинное обучение для предсказаний

---

*Документация обновлена: январь 2025*
