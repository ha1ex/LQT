# Система рейтингов и трекинга

## Обзор

Система рейтингов - это основа для количественного отслеживания качества жизни, предоставляющая данные для анализа трендов, корреляций и валидации гипотез.

## Архитектура системы

### 1. Основные компоненты

#### WeeklyRatingCalendar (`src/components/rating/WeeklyRatingCalendar.tsx`)
**Назначение**: Календарное отображение еженедельных рейтингов

**Функциональность**:
- Календарная сетка с недельными блоками
- Цветовая индикация качества недели
- Быстрый переход к деталям недели
- Навигация по месяцам и годам

**Визуализация**:
- Зеленый - отличная неделя (8-10)
- Желтый - хорошая неделя (6-7)
- Оранжевый - средняя неделя (4-5)
- Красный - плохая неделя (1-3)
- Серый - не оценена

#### WeekDetailModal (`src/components/rating/WeekDetailModal.tsx`)
**Назначение**: Детальное редактирование недельных рейтингов

**Секции**:
- **Метрики** - оценка по каждой метрике (1-10)
- **Заметки** - текстовые комментарии к метрикам
- **Настроение** - общее настроение недели
- **Ключевые события** - важные происшествия
- **Погода** - погодные условия (опционально)

**Валидация**:
- Проверка диапазона рейтингов (1-10)
- Обязательность ключевых метрик
- Автоматический расчет общего балла

#### RatingAnalytics (`src/components/rating/RatingAnalytics.tsx`)
**Назначение**: Аналитика и визуализация данных рейтингов

**Типы анализа**:
- Тренды по времени
- Корреляции между метриками
- Сезонные паттерны
- Распределение настроений
- Лучшие и худшие периоды

### 2. Система метрик

#### Базовые метрики (BASE_METRICS)
```typescript
const BASE_METRICS = [
  {
    id: 'physical_health',
    name: 'Физическое здоровье',
    icon: '💪',
    description: 'Общее самочувствие, энергия, физическая форма',
    category: 'health'
  },
  {
    id: 'mental_health',
    name: 'Ментальное здоровье',
    icon: '🧠',
    description: 'Эмоциональное состояние, стресс, психологический комфорт',
    category: 'health'
  },
  {
    id: 'sleep_quality',
    name: 'Качество сна',
    icon: '😴',
    description: 'Продолжительность и качество сна',
    category: 'health'
  },
  {
    id: 'nutrition',
    name: 'Питание',
    icon: '🥗',
    description: 'Качество питания, соблюдение режима',
    category: 'health'
  },
  {
    id: 'work_satisfaction',
    name: 'Удовлетворенность работой',
    icon: '💼',
    description: 'Вовлеченность, продуктивность, баланс',
    category: 'work'
  },
  {
    id: 'relationships',
    name: 'Отношения',
    icon: '❤️',
    description: 'Качество личных и профессиональных отношений',
    category: 'social'
  },
  {
    id: 'personal_growth',
    name: 'Личностный рост',
    icon: '📚',
    description: 'Обучение, развитие навыков, самосовершенствование',
    category: 'development'
  },
  {
    id: 'leisure_time',
    name: 'Свободное время',
    icon: '🎯',
    description: 'Хобби, развлечения, отдых',
    category: 'lifestyle'
  },
  {
    id: 'financial_stability',
    name: 'Финансовая стабильность',
    icon: '💰',
    description: 'Финансовое благополучие, планирование',
    category: 'financial'
  },
  {
    id: 'peace_of_mind',
    name: 'Спокойствие ума',
    icon: '🧘',
    description: 'Внутренний покой, баланс, осознанность',
    category: 'mental'
  }
];
```

#### Категории метрик
- **health** - Здоровье (физическое и ментальное)
- **work** - Работа и карьера
- **social** - Социальные отношения
- **development** - Личностное развитие
- **lifestyle** - Образ жизни
- **financial** - Финансы
- **mental** - Ментальное благополучие

### 3. Структура данных

#### WeeklyRating
```typescript
interface WeeklyRating {
  id: string;                    // Уникальный идентификатор
  weekNumber: number;            // Номер недели в году
  startDate: Date;               // Начало недели (понедельник)
  endDate: Date;                 // Конец недели (воскресенье)
  ratings: Record<string, number>; // metricId -> rating (1-10)
  notes: Record<string, string>;   // metricId -> note
  mood: 'excellent' | 'good' | 'neutral' | 'poor' | 'terrible';
  keyEvents: string[];           // Ключевые события недели
  weather?: string;              // Погодные условия
  overallScore: number;          // Общий балл недели (1-10)
  createdAt: Date;               // Дата создания
  updatedAt: Date;               // Дата последнего обновления
}
```

#### CalendarDayData
```typescript
interface CalendarDayData {
  date: Date;
  hasRating: boolean;
  overallScore?: number;
  mood?: WeeklyRating['mood'];
}
```

#### WeeklyRatingAnalytics
```typescript
interface WeeklyRatingAnalytics {
  averageByMetric: Record<string, number>;
  trendsOverTime: Array<{
    weekNumber: number;
    averageScore: number;
    date: string;
  }>;
  bestWeek: WeeklyRating | null;
  worstWeek: WeeklyRating | null;
  moodDistribution: Record<WeeklyRating['mood'], number>;
  seasonalTrends: Record<string, number>;
}
```

### 4. Компоненты трекинга

#### QuickEmojiRating (`src/components/tracker/QuickEmojiRating.tsx`)
**Назначение**: Быстрая оценка с помощью эмодзи

**Функциональность**:
- Интуитивный интерфейс с эмодзи
- Быстрая оценка без детального ввода
- Автоматическое сохранение
- Подходит для мобильных устройств

**Шкала эмодзи**:
- 😢 (1-2) - Очень плохо
- 😞 (3-4) - Плохо
- 😐 (5-6) - Нейтрально
- 😊 (7-8) - Хорошо
- 😍 (9-10) - Отлично

#### StreakSystem (`src/components/tracker/StreakSystem.tsx`)
**Назначение**: Система мотивационных серий

**Типы серий**:
- **Серия оценок** - количество недель подряд с оценками
- **Серия роста** - количество недель подряд с улучшением
- **Серия стабильности** - количество недель с высокими оценками

**Вознаграждения**:
- Бейджи за достижения
- Визуальные эффекты
- Мотивационные сообщения

#### PersonalGoals (`src/components/tracker/PersonalGoals.tsx`)
**Назначение**: Управление персональными целями

**Типы целей**:
- Достижение определенного рейтинга по метрике
- Поддержание стабильности
- Улучшение тренда

#### MetricNotes (`src/components/tracker/MetricNotes.tsx`)
**Назначение**: Детальные заметки к метрикам

**Функциональность**:
- Богатый текстовый редактор
- Теги и категоризация
- Поиск по заметкам
- Экспорт данных

## Алгоритмы и вычисления

### 1. Расчет общего балла
```typescript
const calculateOverallScore = (ratings: Record<string, number>): number => {
  const values = Object.values(ratings).filter(v => v > 0);
  return values.length > 0 
    ? parseFloat((values.reduce((sum, val) => sum + val, 0) / values.length).toFixed(1))
    : 0;
};
```

### 2. Корреляционный анализ
```typescript
const calculatePearsonCorrelation = (x: number[], y: number[]): number => {
  const n = x.length;
  if (n !== y.length || n < 3) return 0;
  
  const sumX = x.reduce((a, b) => a + b, 0);
  const sumY = y.reduce((a, b) => a + b, 0);
  const sumXY = x.reduce((sum, xi, i) => sum + xi * y[i], 0);
  const sumX2 = x.reduce((sum, xi) => sum + xi * xi, 0);
  const sumY2 = y.reduce((sum, yi) => sum + yi * yi, 0);
  
  const numerator = n * sumXY - sumX * sumY;
  const denominator = Math.sqrt((n * sumX2 - sumX * sumX) * (n * sumY2 - sumY * sumY));
  
  return denominator === 0 ? 0 : numerator / denominator;
};
```

### 3. Анализ трендов
```typescript
const calculateTrend = (values: number[]): 'up' | 'down' | 'stable' => {
  if (values.length < 2) return 'stable';
  
  const firstHalf = values.slice(0, Math.floor(values.length / 2));
  const secondHalf = values.slice(Math.floor(values.length / 2));
  
  const firstAvg = firstHalf.reduce((a, b) => a + b, 0) / firstHalf.length;
  const secondAvg = secondHalf.reduce((a, b) => a + b, 0) / secondHalf.length;
  
  const difference = secondAvg - firstAvg;
  
  if (Math.abs(difference) < 0.5) return 'stable';
  return difference > 0 ? 'up' : 'down';
};
```

### 4. Сезонный анализ
```typescript
const calculateSeasonalTrends = (ratings: WeeklyRating[]): Record<string, number> => {
  const seasons = {
    winter: [] as number[],
    spring: [] as number[],
    summer: [] as number[],
    autumn: [] as number[]
  };
  
  ratings.forEach(rating => {
    const month = rating.startDate.getMonth() + 1;
    let season: keyof typeof seasons;
    
    if (month >= 12 || month <= 2) season = 'winter';
    else if (month >= 3 && month <= 5) season = 'spring';
    else if (month >= 6 && month <= 8) season = 'summer';
    else season = 'autumn';
    
    seasons[season].push(rating.overallScore);
  });
  
  return Object.entries(seasons).reduce((acc, [season, scores]) => {
    acc[season] = scores.length > 0 
      ? scores.reduce((sum, score) => sum + score, 0) / scores.length 
      : 0;
    return acc;
  }, {} as Record<string, number>);
};
```

## Хуки и утилиты

### useWeeklyRatings
**Назначение**: Управление данными еженедельных рейтингов

**Основные методы**:
- `updateWeekRating(date: Date, updates: Partial<WeeklyRating>)` - обновление рейтинга
- `updateMetricRating(date: Date, metricId: string, rating: number, note?: string)` - обновление конкретной метрики
- `getCurrentWeekRating()` - получение рейтинга текущей недели
- `getAnalytics()` - расчет аналитики
- `generateTestData()` - генерация тестовых данных

**Навигация**:
- `goToNextWeek()` - переход к следующей неделе
- `goToPreviousWeek()` - переход к предыдущей неделе
- `goToCurrentWeek()` - переход к текущей неделе
- `goToWeek(date: Date)` - переход к конкретной неделе

### dataAdapter
**Назначение**: Адаптация данных между форматами

**Функции**:
- `adaptWeeklyRatingsToMockData()` - конвертация в формат для отображения
- `createEmptyMockData()` - создание пустой структуры
- `formatWeekRange()` - форматирование диапазона недели

## Интеграция с другими системами

### 1. Система стратегий
- Предоставляет текущие значения метрик для гипотез
- Используется для валидации результатов экспериментов
- Данные трендов помогают в формулировании новых гипотез

### 2. ИИ система
- Данные рейтингов анализируются ИИ для поиска паттернов
- ИИ предоставляет рекомендации на основе трендов
- Автоматическое выявление корреляций и аномалий

### 3. Dashboard
- Основной источник данных для обзорных метрик
- Визуализация ключевых показателей
- Быстрый доступ к проблемным областям

## Аналитические компоненты

### CorrelationAnalysis (`src/components/tracker/CorrelationAnalysis.tsx`)
**Назначение**: Анализ взаимосвязей между метриками

**Возможности**:
- Матрица корреляций между всеми метриками
- Визуализация силы связей
- Выявление неожиданных корреляций
- Рекомендации на основе корреляций

### PersonalInsights (`src/components/tracker/PersonalInsights.tsx`)
**Назначение**: Персональные инсайты и паттерны

**Типы инсайтов**:
- Лучшие дни недели по метрикам
- Влияние погоды на настроение
- Сезонные паттерны
- Длительные тренды

### WeeklyInsights (`src/components/tracker/WeeklyInsights.tsx`)
**Назначение**: Еженедельные сводки и рекомендации

**Содержание**:
- Сравнение с предыдущими неделями
- Выявление улучшений и ухудшений
- Рекомендации на следующую неделю
- Предупреждения о проблемных трендах

## Визуализация и UI

### Цветовые схемы
```css
/* Рейтинги */
.rating-1-2 { background: hsl(0, 70%, 50%); }     /* Красный */
.rating-3-4 { background: hsl(30, 70%, 50%); }    /* Оранжевый */
.rating-5-6 { background: hsl(60, 70%, 50%); }    /* Желтый */
.rating-7-8 { background: hsl(120, 70%, 50%); }   /* Зеленый */
.rating-9-10 { background: hsl(240, 70%, 50%); }  /* Синий */

/* Тренды */
.trend-up { color: hsl(120, 70%, 40%); }          /* Зеленый рост */
.trend-down { color: hsl(0, 70%, 50%); }          /* Красный спад */
.trend-stable { color: hsl(210, 10%, 50%); }      /* Серая стабильность */
```

### Графические элементы
- **Progress bars** - для отображения прогресса к целям
- **Sparklines** - для мини-графиков трендов
- **Heatmaps** - для календарного отображения
- **Scatter plots** - для корреляционного анализа

## Мобильная адаптация

### Особенности мобильного UI
- Увеличенные области нажатия для рейтингов
- Свайп-жесты для навигации между неделями
- Компактное отображение метрик
- Быстрые действия через bottom sheet

### Touch-оптимизация
- Slider компоненты для рейтингов
- Haptic feedback при взаимодействии
- Оптимизированная клавиатура для ввода заметок

## Экспорт и синхронизация

### Форматы экспорта
- **CSV** - для анализа в Excel/Google Sheets
- **JSON** - для технической интеграции
- **PDF** - для печати отчетов

### Структура экспорта
```csv
Week,Date,Physical_Health,Mental_Health,Sleep_Quality,...,Overall_Score,Notes
W1,2024-01-01,8,7,9,...,8.2,"Good week overall"
W2,2024-01-08,7,8,8,...,7.8,"Stress at work"
```

## Производительность

### Оптимизации
- Ленивая загрузка исторических данных
- Виртуализация длинных списков рейтингов
- Мемоизация тяжелых вычислений аналитики
- Индексирование данных в localStorage

### Кеширование
- Кеширование результатов корреляционного анализа
- Предвычисление агрегированных метрик
- Кеширование графических данных

## Лучшие практики

### 1. Регулярность оценок
- Напоминания о еженедельных оценках
- Мотивационные элементы для поддержания привычки
- Упрощенные формы для быстрого ввода

### 2. Качество данных
- Валидация введенных рейтингов
- Предупреждения о пропущенных неделях
- Возможность корректировки исторических данных

### 3. Анализ и действия
- Регулярный просмотр трендов
- Использование инсайтов для формирования гипотез
- Корреляция с внешними факторами (погода, события)

### 4. Конфиденциальность
- Локальное хранение чувствительных данных
- Опциональная анонимизация экспортируемых данных
- Безопасное удаление данных при необходимости